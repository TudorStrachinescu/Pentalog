package com.tudor.staticVariables;

import java.io.File;

public final class FilePaths {
    public static final String ACCOUNT_FILE_PATH = "src" + File.separator + "main" +
            File.separator + "resources" + File.separator + "accountInfo.txt";
    public static final String USERS_FILE_PATH = "src" + File.separator + "main" +
            File.separator + "resources" + File.separator +"data.txt";
}
